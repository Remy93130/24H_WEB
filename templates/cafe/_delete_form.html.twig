<form method="post" action="{{ path('cafe_delete', {'id': cafe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cafe.id) }}">
    <div class="button-container"><button class="cafe-button red">Supprimer le café</button></div>
</form>
