{% extends 'base.html.twig' %}

{% block title %}Edit Commande{% endblock %}

{% block body %}
	
	<a href="{{ path('commande_index') }}"><div class="button-back"></div></a>
	<div class="form-container w100p row-c-c">
		<div class="form-shape">
			<div class="head">Modifier la commande</div>
			{{ include('commande/_form.html.twig', {'button_label': 'Modifier'}) }}
		    {{ include('commande/_delete_form.html.twig') }}
		</div>
	</div>

    
{% endblock %}
