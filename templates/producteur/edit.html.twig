{% extends 'base.html.twig' %}

{% block title %}Edit Producteur{% endblock %}

{% block body %}
	
	<a href="{{ path('producteur_index') }}"><div class="button-back"></div></a>
	
	<div class="form-container w100p row-c-c">
		<div class="form-shape">
			<div class="head">Editer</div>
    		{{ include('producteur/_form.html.twig', {'button_label': 'Modifier'}) }}
    		{{ include('producteur/_delete_form.html.twig') }}
    	</div>
    </div>


    

    
{% endblock %}
